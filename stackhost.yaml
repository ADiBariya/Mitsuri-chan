user:
  name: Obanai 
  github: Obanai2M 

python_version: "3.10"

commands:
  build:
    - "pip install -r requirements.txt"
  start:
    - "python3 Mitsuri"

repository:
  url: "https://github.com/mikeyy23254/Mitsuri-chan"
  branch: "main"

env:
  - "ENVIRONMENT=production"

custom_commands:
  pre_deploy:
    - "echo Pre-deployment steps"
  post_deploy:
    - "echo Post-deployment steps"